databases:
  - name: meenkari
    databaseName: meenkari
    user: meenkari
    plan: free
    region: singapore

services:
  - type: web
    plan: free
    region: singapore
    name: meenkari
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "daphne meenkari.asgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: meenkari
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4